package ua.drovolskyi.scrum_system.backend.converters;

public class PageToPageDtoConverter {
    // what need to store
    // number of elements in current page = Page.getContent().size()
    // index of current page
    // Page.getTotalPages() - total number of pages
    // Page.getContent() to get the content as List<>
}
